{{- if .Values.apiIngress | default true }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gmr-api
  namespace: {{ .Release.Namespace }}
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
    - host: {{ .Values.domain | default "gmr.void42.internal" }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gmr-api
                port:
                  number: 80
{{- end }}
