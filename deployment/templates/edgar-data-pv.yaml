apiVersion: v1
kind: PersistentVolume
metadata:
  name: edgar-data-pv-{{ .Release.Namespace }}
spec:
  capacity:
    storage: 50Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: 10.44.0.6
    path: /srv/nfs/edgar-data
