apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: gmr
  name: esef-data
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: esef-data-pv
  storageClassName: ""
