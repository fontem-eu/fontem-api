apiVersion: v1
kind: Service
metadata:
  namespace: gmr
  name: edgar-gmr-etl
  labels:
    app: edgar-gmr-etl
spec:
  type: ClusterIP
  selector:
    app: edgar-gmr-etl
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
