apiVersion: v1
name: fontem-api
description: Fontem read API (FastAPI) — serves /api/* over HTTP from the shared
  data tier. Mounts edgar/esef NFS exports read-only for filings + financial-summary
  surfaces. Companion ETL chart at deployment-etl/.
version: 1.0.0
