# Known findings quarantined here so the gitleaks gate starts
# catching NEW secrets without refusing to merge anything until each
# legacy leak is rotated. Every entry below should be removed as its
# secret is rotated out.
#
# Format: <file>:<rule>:<line>

# ── EC sanctions public-URL static token. Not actually a secret —
# the European Commission's public sanctions feed URL includes a
# stable "token=..." query parameter that's the same for every
# consumer. Keep in ignore as a documented false positive.
src/etl/load_eu_sanctions.py:generic-api-key:38
