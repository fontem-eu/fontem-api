[pytest]
markers =
    slow: marks tests that hit real network APIs (deselect with -m "not slow")
    local_e2e: end-to-end tests requiring local EDGAR bulk data (run explicitly)

filterwarnings =
    ignore::DeprecationWarning:edgar.*
    ignore::RuntimeWarning:src.analysis.*
