name: CI/CD
on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: python
    steps:
      - name: Step 1
        run: echo "step 1 ok"
      - name: Step 2
        run: apt-get update && echo "step 2 ok"
      - name: Step 3
        run: apt-get install -y git gcc g++ libpq-dev && echo "step 3 ok"
