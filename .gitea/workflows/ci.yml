name: CI/CD
on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: python
    steps:
      - name: Debug
        run: |
          echo "Hello from CI"
          python3 --version
          which pip
          env | sort | head -30
