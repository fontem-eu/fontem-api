# Data sources
edgartools

# Data & analysis
pandas
numpy
matplotlib

# CLI presentation
rich

# Logging
loguru

# REST API
fastapi
uvicorn[standard]
httpx
dishka>=1.3

# Graph database
neo4j

# RDF — used by the legacy direct-write path (load_us_financials,
# load_eu_listings.load_financials). These keep RdfFilingsWriter
# until they're migrated to event emission too. Sanctions ETL has
# moved off these.
rdflib>=7.0
pyshacl>=0.27

# Event-log architecture: producers emit into events.entity_events
# instead of writing stores directly. See fontem-ontology/MIGRATION.md.
# Pulled from the internal Gitea via git+http; CI build resolves at
# install time.
gmr-event-schemas @ git+http://contribute.void42.internal/golden/gmr-event-schemas.git@main
gmr-events        @ git+http://contribute.void42.internal/golden/gmr-events.git@main

# Relational + spatial (fontem_stats / Eurostat layer)
psycopg[binary]>=3.1

# Observability
prometheus-fastapi-instrumentator
pycountry
